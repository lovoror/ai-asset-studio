# Control machine: the HTTP API, the orchestrator and the CLI/MCP helpers.
# No CUDA: this process only talks to workers over HTTP.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
httpx>=0.27
PyYAML>=6.0
numpy>=1.26
pillow>=10.3
trimesh==4.10.1
pygltflib>=1.0.6

# tests
pytest>=8.0
